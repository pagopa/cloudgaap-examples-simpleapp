doctype html
html
    head
        title Home page
        link(rel='stylesheet', href='https://unpkg.com/mvp.css')

    body
        h1 Your notes:
        if item.length
        - var n=0;
        table
            tr
                th #
                th Title
                th Option

                for data in item
                    - n++
                    tr
                        td= n
                        td
                            a(href=`${global.baseUrl}/read/${data.id}`)=data.title
                        td
                            a(href=`${global.baseUrl}/update/${data.id}`) Update
                            a(href=`${global.baseUrl}/delete/${data.id}`) Delete
    br
    a(href=`${global.baseUrl}/create`) Create note